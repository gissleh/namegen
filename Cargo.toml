[package]
name = "namegen"
version = "0.1.0"
authors = ["Gissleh"]
edition = "2018"

[dependencies]
rand = "0.6.5"
serde = { version = "^1.0.0", optional = true, features = ["derive"] }

[dev-dependencies]
time = "0.1.40"
serde_json = "1.0.44"